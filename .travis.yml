language: node_js
node_js:
  - "14"
install:
  - npm install
script:
  - npm test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: main